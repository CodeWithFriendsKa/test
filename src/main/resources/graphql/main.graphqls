type Person{
    id: ID
    name: String
    age: Int
    contracts: [Contract]
}

type Query{
    persons: [Person]
    personById(id: ID!): Person
}

type Contract{
    id: ID
    name: String
    amount: Int
}